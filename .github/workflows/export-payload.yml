name: Export Current KANG Payload

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/export-payload.yml"

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Concatenate repository text payloads
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p exported
          cat build_payload/part-* > exported/repository-overlay.b64
          cat build_icon/part-* > exported/repository-icon.b64
          sha256sum exported/*.b64 | tee exported/SHA256SUMS.txt
          wc -c exported/*.b64 | tee exported/SIZES.txt
      - name: Upload repository payload copies
        uses: actions/upload-artifact@v4
        with:
          name: KANG_REPOSITORY_PAYLOAD_EXPORT
          path: exported/
          if-no-files-found: error
          retention-days: 7
